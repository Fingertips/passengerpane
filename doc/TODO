- Add some simple functionality to check if an update is available and direct the user to the download page.
- Don't show 'No Selection' in the text fields.- Merge the Aliases text field into the Address field and add documentation about this to the help html file.